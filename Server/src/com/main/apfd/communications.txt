User -> Server
    -> login / logout
    -> status

Server -> user
    -> online / offline

User -> User
    -> broadcast messages
    -> direct messages

commands:
    login user password
    quit/logoff
    msg toUser text
